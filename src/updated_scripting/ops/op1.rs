pub enum Op1 {
    Collect,
    Rev,
    Sort,
    Count,
    First,
    Last,
    MinIn,
    MaxIn,
    Sum,
    Prod,
    AllEqual,
    Flatten,
    Dedup,
    Unique,
}

pub enum Pred1 {
    AllEqual,
}
